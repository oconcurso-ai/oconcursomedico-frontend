{
  "name": "oconcursomedico-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:backend": "cd ../backend && for f in *.gs; do mv \"$f\" \"${f%.gs}.js\"; done && npx @google/clasp push && for f in *.js; do [ \"$f\" = \"appsscript.json\" ] || mv \"$f\" \"${f%.js}.gs\"; done"
  },
  "devDependencies": {
    "lightningcss": "^1.33.0",
    "vite": "^6.0.0"
  }
}